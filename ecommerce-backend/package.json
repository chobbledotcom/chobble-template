{
	"name": "ecommerce-backend",
	"version": "1.0.0",
	"description": "Minimal checkout backend for Stripe and PayPal (Bun Native)",
	"type": "module",
	"main": "server.js",
	"scripts": {
		"start": "bun server.js",
		"test": "bun test"
	},
	"dependencies": {
		"stripe": "^14.0.0"
	}
}
